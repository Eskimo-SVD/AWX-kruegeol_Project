---
- hosts: f-pc5844-debian13.fritz.box
  become: true
#  become_method: enable
  tasks:
    - name: Update the repository cache and update package freecad to latest version using default release
      ansible.builtin.apt:
        name: freecad
        state: latest
#    default_release: squeeze-backports
        update_cache: yes

